<Window
    x:Class="WInstaller.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,0,1,0">
            <ScrollViewer>
                <StackPanel x:Name="SidebarPanel" Margin="12,16,12,16" Spacing="2" />
            </ScrollViewer>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0" Padding="28,24,28,8">
                <ContentControl x:Name="StepContent"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Stretch" />
            </ScrollViewer>

            <Grid Grid.Row="1" Padding="28,12,28,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button x:Name="BackButton" Grid.Column="0" Click="OnBack">Back</Button>

                <TextBlock x:Name="ErrorText"
                           Grid.Column="1"
                           Margin="16,0,16,0"
                           VerticalAlignment="Center"
                           TextWrapping="Wrap"
                           Foreground="{ThemeResource SystemFillColorCriticalBrush}" />

                <Button x:Name="ContinueButton"
                        Grid.Column="2"
                        Style="{StaticResource AccentButtonStyle}"
                        Click="OnContinue">Continue</Button>
            </Grid>
        </Grid>
    </Grid>
</Window>
